#!/usr/bin/env sh
# Full safety net before any commit: types + unit + e2e + visual regression.
# Bypass with `git commit --no-verify` if you genuinely need to.

yarn typecheck && yarn test && yarn test:e2e
